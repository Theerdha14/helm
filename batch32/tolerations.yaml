apiVersion: v1
kind: Pod
metadata:
  name: my-pod
  namespace: helm
spec:
  containers:
  - name: my-container
    image: helmcontainer.azurecr.io/dockerr:latest
  tolerations:
  - key: "special"
    operator: "Equal"
    value: "true"
    effect: "NoSchedule"
