apiVersion: v1
kind: Pod
metadata:
  name: my-pod
  namespace: aks
spec:
  containers:
  - name: my-container
    image: theerdha14/dockercompose-app:latest
  tolerations:
  - key: "special"
    operator: "Equal"
    value: "true"
    effect: "NoSchedule"
